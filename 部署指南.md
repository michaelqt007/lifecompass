# LifeCompass 部署指南

## 方式一：Vercel 一键部署（推荐）⭐

### 步骤

**1. 安装 Vercel CLI**
```bash
npm install -g vercel
```

**2. 登录 Vercel**
```bash
vercel login
```

**3. 部署**
```bash
cd /root/.openclaw.pre-migration/workspace/lifecompass/web
vercel
```

**4. 配置环境变量**

在 Vercel 控制台（https://vercel.com/dashboard）：
- 进入你的项目
- Settings → Environment Variables
- 添加 `DEEPSEEK_API_KEY`
- 可选备用：添加 `DASHSCOPE_API_KEY`

**5. 重新部署**
```bash
vercel --prod
```

### 优点
- ✅ 免费托管
- ✅ 自动 HTTPS
- ✅ 全球 CDN
- ✅ 绑定 GitHub 自动部署

---

## 方式二：Docker 部署

### Dockerfile

创建 `Dockerfile`：

```dockerfile
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN npm run build

EXPOSE 3000

CMD ["npm", "start"]
```

### 构建和运行

```bash
docker build -t lifecompass .
docker run -p 3000:3000 -e DEEPSEEK_API_KEY=your_deepseek_key_here lifecompass
```

---

## 方式三：服务器部署

### 1. 上传代码到服务器

```bash
scp -r web user@your-server:/path/to/lifecompass/
```

### 2. 安装依赖

```bash
cd /path/to/lifecompass/web
npm install
```

### 3. 配置环境变量

```bash
echo 'DEEPSEEK_API_KEY=your_deepseek_key_here' > .env.local
```

### 4. 构建

```bash
npm run build
```

### 5. 使用 PM2 运行

```bash
npm install -g pm2
pm2 start npm --name "lifecompass" -- start
pm2 save
pm2 startup
```

### 6. 配置 Nginx（可选）

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

---

## 环境变量说明

| 变量名 | 说明 | 必填 |
|--------|------|------|
| `DEEPSEEK_API_KEY` | DeepSeek API Key，当前主链路 | ✅ |
| `DASHSCOPE_API_KEY` | DashScope API Key，备用兼容链路 | ❌ |
| `NEXT_PUBLIC_API_URL` | API 地址（默认本地） | ❌ |

---

## 测试清单

部署后检查以下功能：

- [ ] 页面能正常打开
- [ ] 小雨开场白正常显示
- [ ] 文字输入能发送消息
- [ ] 语音输入能录音（浏览器支持时）
- [ ] 语音输出能朗读（浏览器支持时）
- [ ] 对话历史正常滚动
- [ ] 加载状态正常显示

---

## 常见问题

### Q: 语音功能不工作？

**A:** 检查浏览器是否支持 Web Speech API
- Chrome/Edge：支持良好
- Safari：部分支持
- Firefox：需要配置

### Q: API 调用失败？

**A:** 检查：
1. `.env.local` 文件是否存在
2. `DEEPSEEK_API_KEY` 是否正确
3. 如果使用备用链路，`DASHSCOPE_API_KEY` 是否正确
4. 服务器能否访问外网

### Q: 部署后页面空白？

**A:** 打开浏览器控制台查看错误，通常是：
- 环境变量未配置
- API 路径错误
- 构建失败

---

## 后续优化

1. **添加用户系统**：保存对话历史
2. **接入更多大模型**：OpenAI、Anthropic 等
3. **语音优化**：使用 ElevenLabs 等高质量 TTS
4. **数据分析**：记录用户行为，优化对话

---

**部署完成后，你就可以开始找朋友测试了！** 🚀
